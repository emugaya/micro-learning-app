%h2 Click on a Course to view available Lessons.
%br
  .courses
    - if @courses
      .card-columns
        - @courses.each do |course|
          .card.bg-info.mb-3.text-left.text-white
            .card-body
              %h5= course.name
              %p= course.description
              %a.btn.btn-primary{:href =>"/courses/#{course.id}/lessons"} View Lessons
              - if session[:user_id]
                - if @enrolled_courses.include?(course.id)
                  %a.btn.btn-primary{:href =>"/courses/#{course.id}/withdraw"} Withdraw
                - else
                  %a.btn.btn-primary{:href =>"/courses/#{course.id}/enrol"} Enrol
    - else
      %p Sorry, there are no courses to display. Check back later