%h2 #{@category.name} Courses
%br
.courses
  - if @courses
    .card-columns
      - @courses.each do |course|
        .card.bg-info.mb-3.text-left.text-white
          .card-body
            %h5= course.name
            %p= course.description
            %a.btn.btn-primary{:href =>"/courses/#{course.id}/lessons"} View Course
            - if current_user
              - if @enrolled_course_ids.include?(course.id)
                %a.btn.btn-primary{:href =>"/courses/#{course.id}/withdraw"} Withdraw
              - else
                %a.btn.btn-primary{:href =>"/courses/#{course.id}/enrol"} Enrol          
  - else
    %p Ooops, there are no courses to display. Check back later!
